<section class="hero" style="background-image: url('{{ hero_image | default('/uploads/hero.jpg') }}')">
  <div class="hero-overlay">
    <h1 class="hero-title">Premium African Stock Photos & Videos</h1>
    <p class="hero-subtext">{{ hero_subtext }}</p>
    <div class="hero-buttons">
      {% for btn in hero_buttons %}
        <a class="btn" href="{{ btn.url }}">{{ btn.text }}</a>
      {% endfor %}
    </div>
  </div>
</section>
